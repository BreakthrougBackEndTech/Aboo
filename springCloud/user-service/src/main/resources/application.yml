server:
  port: 0

spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888
      
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream 